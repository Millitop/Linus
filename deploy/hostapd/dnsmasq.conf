# Example dnsmasq config so phones connecting to the Pi's access point
# get an IP address automatically. Copy to /etc/dnsmasq.d/gardskort.conf.

interface=wlan0
dhcp-range=192.168.50.10,192.168.50.100,255.255.255.0,24h
domain=wlan
address=/gardskort.local/192.168.50.1
